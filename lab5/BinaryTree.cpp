/*
#include "BinaryTree.h";

/*BinTree::BinTree()
{
	length = 0;
	tree->key = 0;
	tree->parent = nullptr;
	tree->left = nullptr;
	tree->right = nullptr;
}

BinTree::BinTree(int in[25], int len)
{
	int i = 0;
	tree->key = 0;
	tree->parent = nullptr;
	tree->left = nullptr;
	tree->right = nullptr;
	if (tree->parent == nullptr)
	{
		tree->key = in[i];
		i++;
	}
	if (tree->key > in[i])
			(*t) = new node;               
		(*t)->info = a;                 
		(*t)->l = (*t)->r = NULL;       
		return;                         
	

	if (a > (*t)->info) push(a, &(*t)->r); 
	else push(a, &(*t)->l);
}

void BinTree::AddNode(int k)
{

}

void BinTree::Show() const
{
	int i = 0;
	node*
	cout << "Последовательность ключей:\n";
	for (i; i < length; i++)
	{
		cout << nodes[i].key;
		if (i != (length - 1)) cout << ", ";
	}
	cout << endl;
	cout << tree;
	return;
}
*/